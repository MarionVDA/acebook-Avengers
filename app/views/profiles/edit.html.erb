<h1>Editing User information</h1>

<%= simple_form_for(current_user) do |f| %>

  <div class="form-inputs">
    <%= f.input :username, :as => :text, input_html: { autocomplete: "username" } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Update profile" %>
  </div>
<% end %>
